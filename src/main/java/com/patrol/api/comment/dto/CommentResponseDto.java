package com.patrol.api.Comment.dto;
/*
import com.patrol.domain.Comment.entity.Comment;
import lombok.Getter;

@Getter
public class CommentResponseDto {
    private Long id;
    private String content;
    private Long authorId;
    private Long lostPostId;
    private Long findPostId;

    public CommentResponseDto(Comment comment) {
        this.id = comment.getId();
        this.content = comment.getContent();
        this.authorId = comment.getAuthor().getId();
        this.lostPostId = comment.getLostPost() != null ? comment.getLostPost().getLostId() : null;
        this.findPostId = comment.getLostFound() != null ? comment.getLostFound().getFoundId() : null;
    }
}
*/